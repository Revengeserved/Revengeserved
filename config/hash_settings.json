{
  "version": "0.1.0",
  "primary_hash": "sha256",
  "enabled": false,
  "defer_large_files": true,
  "large_file_threshold_bytes": 1073741824,
  "allowed_hash_types": [
    "sha256",
    "provider_hash",
    "perceptual_hash"
  ],
  "notes": "SHA-256 is the only primary identity hash. Perceptual hashes are never proof of exact duplicates. Hashing remains disabled until a later explicit approval gate."
}
